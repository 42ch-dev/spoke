/**
 * AUTO-GENERATED FROM JSON SCHEMA - DO NOT MODIFY
 * Source: schemas/
 * Generated by: pnpm run codegen
 */

/**
 * Remote op reply. Use payload (success) OR error — not both.
 */
export type ConnectInvokeResponse =
  | {
      /**
       * Echo of request session_id.
       */
      session_id: string;
      /**
       * Echo of request sequence.
       */
      sequence: number;
      /**
       * Echo of request request_id.
       */
      request_id: string;
      /**
       * Ops response success envelope (or product-defined success body for non-core op).
       */
      payload: {
        [k: string]: unknown | undefined;
      };
      /**
       * base64url (no padding) of the 64-byte Ed25519 signature over the RFC 8785 JCS canonicalized signed object (spoke-connect-invoke-response-jcs-v1).
       */
      signature: string;
      extensions: ExtensionMap;
    }
  | {
      /**
       * Echo of request session_id.
       */
      session_id: string;
      /**
       * Echo of request sequence.
       */
      sequence: number;
      /**
       * Echo of request request_id.
       */
      request_id: string;
      error: ErrorEnvelope;
      /**
       * base64url (no padding) of the 64-byte Ed25519 signature over the RFC 8785 JCS canonicalized signed object (spoke-connect-invoke-response-jcs-v1).
       */
      signature: string;
      extensions: ExtensionMap2;
    };

/**
 * Product namespace bag.
 */
export interface ExtensionMap {
  [k: string]:
    | {
        [k: string]: unknown | undefined;
      }
    | undefined;
}
/**
 * Failure: shared error envelope.
 */
export interface ErrorEnvelope {
  /**
   * Machine-readable error code.
   */
  code: string;
  /**
   * Human-readable error message.
   */
  message: string;
  /**
   * Optional structured error context.
   */
  details?: {
    [k: string]: unknown | undefined;
  };
  extensions: ExtensionMap1;
}
/**
 * Product namespace bag keyed by product-chosen ids matching ^[a-z][a-z0-9_-]*$. Values are opaque JSON objects. Adapters MUST preserve unknown namespaces and keys on round-trip.
 */
export interface ExtensionMap1 {
  [k: string]:
    | {
        [k: string]: unknown | undefined;
      }
    | undefined;
}
/**
 * Product namespace bag keyed by product-chosen ids matching ^[a-z][a-z0-9_-]*$. Values are opaque JSON objects. Adapters MUST preserve unknown namespaces and keys on round-trip.
 */
export interface ExtensionMap2 {
  [k: string]:
    | {
        [k: string]: unknown | undefined;
      }
    | undefined;
}
