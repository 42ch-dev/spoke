/**
 * AUTO-GENERATED FROM JSON SCHEMA - DO NOT MODIFY
 * Source: schemas/
 * Generated by: pnpm run codegen
 */

/**
 * Established session context (wire snapshot). Runtimes may keep richer local state; the public wire shape stays this table.
 */
export interface ConnectSession {
  /**
   * Opaque session id (UUID recommended; not schema-enforced).
   */
  session_id: string;
  /**
   * Peer that dialed / sent first hello.
   */
  initiator_peer_id: string;
  /**
   * Peer that accepted.
   */
  responder_peer_id: string;
  /**
   * Session open time (UTC).
   */
  opened_at: string;
  /**
   * Intersection (or agreed subset) of both hosts' capabilities[]; MUST include spoke-connect when both declare it.
   *
   * @minItems 1
   */
  negotiated_capabilities: [string, ...string[]];
  /**
   * First invoke request uses sequence = initial_sequence (0 for protocol version 1).
   */
  initial_sequence: 0;
  extensions: ExtensionMap;
}
/**
 * Product namespace bag.
 */
export interface ExtensionMap {
  [k: string]:
    | {
        [k: string]: unknown | undefined;
      }
    | undefined;
}
