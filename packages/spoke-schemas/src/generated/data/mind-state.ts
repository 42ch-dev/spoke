/**
 * AUTO-GENERATED FROM JSON SCHEMA - DO NOT MODIFY
 * Source: schemas/
 * Generated by: pnpm run codegen
 */

/**
 * First-class temporal mental-state record under optional capability l5-mind. Strictly derivative of the holder KnowledgeEntry's modules.mental / modules.belief (settled home); never a second authority. Distinct from KnowledgeEntry ontology labels such as entry_type: "character" or profile "mind". Engines (belief revision, ToM inference) are product-owned; SPOKE owns the wire shape.
 */
export interface MindState {
  /**
   * Wire schema version (integer >= 1).
   */
  schema_version: number;
  /**
   * Stable MindState id (opaque to protocol).
   */
  mind_state_id: string;
  /**
   * Reference to the mental actor's KnowledgeEntry (entry_id). This record is a temporal derivative of that entry's modules.mental / modules.belief.
   */
  holder_entry_id: string;
  /**
   * Human-stable label.
   */
  canonical_name?: string;
  /**
   * When the mental state held (RFC 3339 UTC datetime).
   */
  occurred_at?: string;
  /**
   * Opaque ordering hint within a mental-state timeline (products define grammar).
   */
  sort_key?: string;
  snapshot?: MentalFieldMap;
  /**
   * Derivative field-level change records since the prior mental state (temporal-delta presentation only).
   */
  deltas?: MindDelta[];
  source_anchor?: SourceAnchor;
  /**
   * RFC 3339 UTC datetime string.
   */
  created_at?: string;
  /**
   * RFC 3339 UTC datetime string.
   */
  updated_at?: string;
  extensions: ExtensionMap;
}
/**
 * Derivative full-field snapshot of the holder's mental state at this point in time.
 */
export interface MentalFieldMap {
  [k: string]: unknown | undefined;
}
/**
 * Field-level change record for temporal-delta presentation of mental-state changes; presentation only, not authoritative state. Authoritative values live on the holder KnowledgeEntry under modules.mental / modules.belief.
 */
export interface MindDelta {
  /**
   * Dot-path or JSON Pointer to the changed field within modules.mental or modules.belief.
   */
  path: string;
  previous?: unknown;
  next?: unknown;
}
/**
 * Optional manuscript or scene anchor.
 */
export interface SourceAnchor {
  /**
   * Wire schema version (integer >= 1).
   */
  schema_version: number;
  /**
   * Opaque source locator; products define grammar.
   */
  source_id: string;
  span?: SourceSpan;
  /**
   * Optional human label for the anchor.
   */
  label?: string;
  /**
   * Optional MIME type of the referenced source.
   */
  mime_type?: string;
  extensions: ExtensionMap;
}
/**
 * Optional byte or character span within the source.
 */
export interface SourceSpan {
  /**
   * Start offset (inclusive).
   */
  start: number;
  /**
   * End offset (exclusive).
   */
  end: number;
}
/**
 * Product namespace bag keyed by product-chosen ids matching ^[a-z][a-z0-9_-]*$. Values are opaque JSON objects. Adapters MUST preserve unknown namespaces and keys on round-trip.
 */
export interface ExtensionMap {
  [k: string]:
    | {
        [k: string]: unknown | undefined;
      }
    | undefined;
}
