/**
 * AUTO-GENERATED FROM JSON SCHEMA - DO NOT MODIFY
 * Source: schemas/
 * Generated by: pnpm run codegen
 */

/**
 * Propose provisional KnowledgeEntry candidates from referenced source material; returns ExtractResponse.
 */
export interface ExtractRequest {
  /**
   * Non-empty opaque caller-supplied correlation identity for this extraction batch. Echoed verbatim in ExtractResponse run.run_id; not a durable run record.
   */
  run_id: string;
  /**
   * Non-empty referenced source list. Extraction range is this list plus each anchor's optional span (absent span = the referenced artifact as a whole). No inline-content field.
   *
   * @minItems 1
   */
  sources: [SourceAnchor, ...SourceAnchor[]];
  /**
   * Optional candidate-type hints. Advisory open vocabulary — not a closed taxonomy and not a post-filter.
   */
  entry_types?: string[];
  extensions?: ExtensionMap1;
}
/**
 * Pointer to a source artifact span (manuscript, scene, external URI).
 */
export interface SourceAnchor {
  /**
   * Wire schema version (integer >= 1).
   */
  schema_version: number;
  /**
   * Opaque source locator; products define grammar.
   */
  source_id: string;
  span?: SourceSpan;
  /**
   * Optional human label for the anchor.
   */
  label?: string;
  /**
   * Optional MIME type of the referenced source.
   */
  mime_type?: string;
  extensions: ExtensionMap;
}
/**
 * Optional byte or character span within the source.
 */
export interface SourceSpan {
  /**
   * Start offset (inclusive).
   */
  start: number;
  /**
   * End offset (exclusive).
   */
  end: number;
}
/**
 * Product namespace bag keyed by product-chosen ids matching ^[a-z][a-z0-9_-]*$. Values are opaque JSON objects. Adapters MUST preserve unknown namespaces and keys on round-trip.
 */
export interface ExtensionMap {
  [k: string]:
    | {
        [k: string]: unknown | undefined;
      }
    | undefined;
}
/**
 * Product namespace bag keyed by product-chosen ids matching ^[a-z][a-z0-9_-]*$. Values are opaque JSON objects. Adapters MUST preserve unknown namespaces and keys on round-trip.
 */
export interface ExtensionMap1 {
  [k: string]:
    | {
        [k: string]: unknown | undefined;
      }
    | undefined;
}
