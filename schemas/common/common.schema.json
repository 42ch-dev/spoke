{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://spoke42.invalid/schemas/common/common.schema.json",
  "title": "SPOKE Common Types",
  "description": "Shared type definitions for SPOKE wire schemas.",
  "definitions": {
    "SchemaVersion": {
      "type": "integer",
      "minimum": 1,
      "description": "Wire schema version (integer >= 1)."
    },
    "Timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "RFC 3339 UTC datetime string."
    },
    "ExtensionMap": {
      "type": "object",
      "description": "Product namespace bag keyed by product id (nexus, creader, ...). Values are opaque JSON objects. Adapters MUST preserve unknown namespaces and keys on round-trip.",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": true
      },
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9_-]*$"
      }
    },
    "SourceSpan": {
      "type": "object",
      "required": ["start", "end"],
      "properties": {
        "start": {
          "type": "number",
          "description": "Start offset (inclusive)."
        },
        "end": {
          "type": "number",
          "description": "End offset (exclusive)."
        }
      },
      "additionalProperties": false
    },
    "TimelineScale": {
      "type": "string",
      "description": "L5 Timeline projection tier (open string). Core vocabulary (documented, not enforced): brief, narrative, moment. Wire field name timeline_scale — not tier or projection."
    },
    "Scope": {
      "type": "object",
      "required": ["scope_id"],
      "properties": {
        "scope_id": {
          "type": "string",
          "description": "Protocol-neutral opaque selector. Products map World/Book/chapter/manuscript ids via adapters or op extensions."
        },
        "knowledge_entry_ids": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Optional narrow scope to explicit KnowledgeEntries."
        },
        "entry_types": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Optional filter by open entry_type vocabulary."
        },
        "timeline_event_ids": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Optional narrow scope to explicit L5 TimelineEvent ids."
        },
        "source_id": {
          "type": "string",
          "description": "Optional provenance or manuscript locator scope."
        },
        "timeline_scale": {
          "$ref": "#/definitions/TimelineScale",
          "description": "Optional L5 tier filter (brief, narrative, moment)."
        }
      },
      "additionalProperties": false
    }
  }
}
