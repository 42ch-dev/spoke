{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://spoke42.invalid/schemas/connect/connect-hello.schema.json",
  "title": "ConnectHello",
  "description": "Handshake envelope: signed self-description exchange (peer_id + embedded HostCapabilityManifest).",
  "type": "object",
  "required": [
    "protocol_version",
    "peer_id",
    "nonce",
    "host",
    "signature",
    "extensions"
  ],
  "properties": {
    "protocol_version": {
      "type": "integer",
      "minimum": 1,
      "description": "Connect protocol version (not data schema_version). Protocol version 1 is current."
    },
    "peer_id": {
      "type": "string",
      "minLength": 1,
      "description": "Sender network identity — protocol_version 1: libp2p identity-spec PeerId string for Ed25519 (base58btc identity multihash of the protobuf PublicKey); opaque to protocol logic; trust root for the noise-peerid allowlist."
    },
    "nonce": {
      "type": "string",
      "minLength": 16,
      "description": "Single-use replay nonce. Bound into the signed object."
    },
    "peer_nonce": {
      "type": "string",
      "minLength": 16,
      "description": "Responder-only dial binding: the initiator's nonce, echoed by the responder and bound into its signed object (spoke-connect-hello-jcs-v1). Absent in initiator hellos (the initiator sends first and has no peer nonce); initiators MUST reject a responder hello whose peer_nonce does not equal their own nonce."
    },
    "host": {
      "$ref": "https://spoke42.invalid/schemas/data/host-capability-manifest.schema.json",
      "description": "Full embedded manifest (includes host.extensions). Part of the signed object."
    },
    "signature": {
      "type": "string",
      "minLength": 1,
      "description": "base64url (no padding) of raw signature bytes over the JCS-canonicalized signed object (spoke-connect-hello-jcs-v1)."
    },
    "extensions": {
      "$ref": "https://spoke42.invalid/schemas/common/common.schema.json#/definitions/ExtensionMap",
      "description": "Product bag; not covered by the signature."
    }
  },
  "additionalProperties": false
}
