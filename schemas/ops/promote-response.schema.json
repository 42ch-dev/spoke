{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://spoke42.invalid/schemas/ops/promote-response.schema.json",
  "title": "PromoteResponse",
  "description": "Promoted KnowledgeEntry or wire error. Use knowledge_entry OR error — not both.",
  "oneOf": [
    {
      "type": "object",
      "required": ["knowledge_entry"],
      "properties": {
        "knowledge_entry": {
          "$ref": "https://spoke42.invalid/schemas/data/knowledge-entry.schema.json",
          "description": "Success: promoted KnowledgeEntry."
        },
        "superseded_id": {
          "type": "string",
          "description": "Optional id of KnowledgeEntry superseded during merge."
        },
        "extensions": {
          "$ref": "https://spoke42.invalid/schemas/common/common.schema.json#/definitions/ExtensionMap",
          "description": "Optional transport metadata."
        }
      },
      "additionalProperties": false
    },
    {
      "type": "object",
      "required": ["error"],
      "properties": {
        "error": {
          "$ref": "https://spoke42.invalid/schemas/common/error-envelope.schema.json",
          "description": "Failure: shared error envelope."
        },
        "extensions": {
          "$ref": "https://spoke42.invalid/schemas/common/common.schema.json#/definitions/ExtensionMap",
          "description": "Optional transport metadata."
        }
      },
      "additionalProperties": false
    }
  ]
}
