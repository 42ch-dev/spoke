{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://spoke42.invalid/schemas/ops/upsert-response.schema.json",
  "title": "UpsertResponse",
  "description": "Persisted Keyblock view or wire error. Use keyblocks OR error — not both.",
  "oneOf": [
    {
      "type": "object",
      "required": ["keyblocks"],
      "properties": {
        "keyblocks": {
          "type": "array",
          "items": {
            "$ref": "https://spoke42.invalid/schemas/data/keyblock.schema.json"
          },
          "description": "Success: persisted Keyblocks."
        },
        "rejected": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UpsertRejected"
          },
          "description": "Optional per-Keyblock rejections (success branch only)."
        },
        "extensions": {
          "$ref": "https://spoke42.invalid/schemas/common/common.schema.json#/definitions/ExtensionMap",
          "description": "Optional transport metadata."
        }
      },
      "additionalProperties": false
    },
    {
      "type": "object",
      "required": ["error"],
      "properties": {
        "error": {
          "$ref": "https://spoke42.invalid/schemas/common/error-envelope.schema.json",
          "description": "Failure: shared error envelope."
        },
        "extensions": {
          "$ref": "https://spoke42.invalid/schemas/common/common.schema.json#/definitions/ExtensionMap",
          "description": "Optional transport metadata."
        }
      },
      "additionalProperties": false
    }
  ],
  "definitions": {
    "UpsertRejected": {
      "type": "object",
      "required": ["keyblock_id", "code", "message"],
      "properties": {
        "keyblock_id": {
          "type": "string",
          "description": "Rejected Keyblock id."
        },
        "code": {
          "type": "string",
          "description": "Machine-readable rejection code."
        },
        "message": {
          "type": "string",
          "description": "Human-readable rejection message."
        }
      },
      "additionalProperties": false
    }
  }
}
